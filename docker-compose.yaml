version: '3.9'
services:
  web:
    image: nginx
    container_name: web_default
    restart: unless-stopped
    networks:
      - nginx-proxy
    expose:
      - '80'
    environment:
      - VIRTUAL_HOST=web_default.localhost

networks:
  nginx-proxy:
    external: true
